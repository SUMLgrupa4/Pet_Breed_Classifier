# Virtual environments (e.g. created by venv or conda)
.venv/
venv/

# Training artifacts and logs
models/**/*.log
outputs/
*.tmp

# Python cache and compiled files
__pycache__/
*.py[cod]
*.so
*.log

# Training data (but keep metadata)
data/pet_breeds/
data/splits/

# Keep essential model files for deployment
# models/autogluon_model/model.ckpt - KEEP FOR DEPLOYMENT
# models/**/*.pkl - KEEP FOR DEPLOYMENT  
# data/metadata/ - KEEP FOR DEPLOYMENT

# Model checkpoints and weights
*.ckpt
*.h5
*.pth
*.pt

